name: binance_spot_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  asn1lib: ^1.5.8
  convert: 3.1.2
  cryptography: ^2.7.0
  http: ^1.2.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
